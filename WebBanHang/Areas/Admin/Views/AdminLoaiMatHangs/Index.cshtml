@model IEnumerable<WebBanHang.Models.LoaiMatHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiMH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiMH)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDLoaiMH }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDLoaiMH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDLoaiMH })
        </td>
    </tr>
}

</table>
