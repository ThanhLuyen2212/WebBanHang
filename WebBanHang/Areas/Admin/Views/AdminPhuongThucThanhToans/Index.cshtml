@model IEnumerable<WebBanHang.Models.PhuongThucThanhToan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenPT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenPT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDPT }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDPT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDPT })
        </td>
    </tr>
}

</table>
